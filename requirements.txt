# Dependencias do controlador fuzzy (fuzzy.py)
# Pacote PyPI "scikit-fuzzy" e importado no codigo como "skfuzzy".
# Combinacao testada: Python 3.13 / Windows 11.
#   Instalacao:  python -m pip install -r requirements.txt

numpy>=2.0,<3
scipy>=1.11
scikit-fuzzy>=0.5,<0.6
matplotlib>=3.8
paho-mqtt>=1.6,<3
networkx